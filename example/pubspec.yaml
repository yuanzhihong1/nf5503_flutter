name: nf5503_flutter_example
description: Demonstrates the NF5503 scanner and printer Flutter plugin.
publish_to: 'none'

environment:
  sdk: ^3.12.1

dependencies:
  cupertino_icons: ^1.0.8
  flutter:
    sdk: flutter
  nf5503_flutter:
    path: ../

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter

flutter: {}
